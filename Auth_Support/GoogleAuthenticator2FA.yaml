contexts:
  - name: authuser
    key: id
    records: 1
    authentication:
      all:
        target: field-user
        field: username
options:
  authentication:
    storing: passkey
connection:
  db-class: PDO
debug: 2
